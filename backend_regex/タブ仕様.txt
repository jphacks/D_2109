命名規則は警告だよね
=> どうやって示そう

・同じブロック内にタブとスペースのインデントが混在しているとエラーが出る

インデント設定
・半角スペース4文字
・タブ文字 - x文字

・\tの分解 => 半角だけにする(タブ文字×[x文字])

indent値をopとする 
\tを' '*indent値にする(一次的に)


・行頭を見る

・defやclassが表れる[行頭文字数1+indent値]
・defが表れる[行頭文字数1+indent値|行頭文字数2+indent値]
・前の行頭文字数と違う=>pop[行頭文字数1+indent値]

・起点値

fileobj = open("def_sample.py", "r", encoding="utf_8")

    lst=[]
    for line in fileobj.readlines():
      lst.append(line)
    print(lst)


・\tに戻す(タブ設定の場合)